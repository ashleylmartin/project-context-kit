# project-context-kit — project memory

## Session State
- **Session:** 2026-09-09 (s1, end) · `main` · bootstrapped project memory and fixed the local-runtime-cache/memory-tool collision bug (session-start, wrap-up, doctor, bootstrap).

## Gotchas
- The *installed* plugin (`~/.snowflake/cortex/plugins/project-context-kit`) is a separate copy from this dev repo — fixes here need a release + reinstall before other projects see them.

## Next Session
- Cut a release for the memory-cache-collision fix, then reinstall the plugin globally.
